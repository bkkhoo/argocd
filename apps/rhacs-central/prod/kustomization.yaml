# values files are located relative to the chart path specified in the sources.path
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base

patches:
  - target:
      group: argoproj.io
      version: v1alpha1
      kind: Application
      name: rhacs-central
    patch: |
      - op: add
        path: /spec/sources/0/helm/valueFiles
        value:
          - ../../apps/rhacs-central/prod/values.yaml
...
