---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: openshift-web-terminal
  namespace: openshift-gitops
  labels:
    applications: openshift-web-terminal
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true  # prevent child resources from being deleted, when the parent Application is deleted
  generators:
    - clusters: {}     # all clusters
  template:
    metadata:
      name: "{{ name }}-openshift-web-terminal"
      labels:
        applications: openshift-web-terminal
    spec:
      project: iac-gitops
      source:
        repoURL: https://github.com/bkkhoo/argocd.git
        targetRevision: main
        path: kustomize-deployments/openshift-web-terminal/base/
      destination:
        server: "{{ server }}"
        namespace: openshift-web-terminal
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
...
