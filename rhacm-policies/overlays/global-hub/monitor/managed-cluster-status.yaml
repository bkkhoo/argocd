# ---
# apiVersion: cluster.open-cluster-management.io/v1
# kind: ManagedCluster
# status:
#   conditions:
#     - type: ManagedClusterConditionAvailable
#       reason: ManagedClusterAvailable
#       status: "True"
# ...


---
object-templates-raw: |
  {{- $mc := lookup "cluster.open-cluster-management.io/v1" "ManagedCluster" "" "" }}
  {{- if gt (len $mc.items) 1 }}
  - complianceType: musthave
    objectDefinition:
      apiVersion: cluster.open-cluster-management.io/v1
      kind: ManagedCluster
      status:
        conditions:
          - type: ManagedClusterConditionAvailable
            reason: ManagedClusterAvailable
            status: "True"
  {{- end }}
...
