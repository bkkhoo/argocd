---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app-1
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "test-chart-1.setLabels" .Values.labels | indent 4 }}
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      {{- include "test-chart-1.setLabels" .Values.labels | indent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      name: test-app-1
      labels:
        {{- include "test-chart-1.setLabels" .Values.labels | indent 8 }}
    spec:
      containers:
      - name: test-app-1
        image: quay.io/bkhoo/python3-async:2.0
        command:
          - "python"
          - "-m"
          - "http.server"
          - "8080"
        env:
          - name: HOME
            value: {{ .Values.env.HOME }}
          - name: CONFIG_FILE
            value: {{ .Values.env.CONFIG_FILE }}
      serviceAccountName: {{ .Values.serviceAccount }}
...
