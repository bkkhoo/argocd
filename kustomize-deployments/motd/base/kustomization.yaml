---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# all resources are deployed to this namespace; do not set metadata.namespace property on the resources
namespace: openshift

resources:
  - configmap.yaml
...
